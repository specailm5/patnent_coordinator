# Authentication
JWT_SECRET=your_jwt_secret

# Backup encryption (must be exactly 32 characters)
BACKUP_ENCRYPTION_KEY=your_backup_encryption_key

# API base URL (for development, defaults to http://localhost:3000/api if not set)
API_BASE=http://localhost:3000/api

# Node environment (development or production)
NODE_ENV=production

